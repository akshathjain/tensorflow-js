### Project Setup
- `mkdir tensorflow-js`
- `cd tensorflow-js`
- `npm init`
- `npm install @tensorflow/tfjs-node`

### Notes
- Safari (to enable video access): `Developer -> Web RTC -> Allow Media Capture on Insecure Sites`